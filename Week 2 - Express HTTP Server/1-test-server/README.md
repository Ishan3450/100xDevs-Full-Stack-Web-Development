# Ways to create a new node project and creating new HTTP server with express

```bash
npm init -y
```
package.json will be created after executing above command in the terminal

To install express
```bash
npm install express
```